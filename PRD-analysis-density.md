# PRD: 分析密度强制门禁

## 状态
完成 - 2026-07-31

## 背景

Meta 2026-07-30 报告与同日 SK海力士报告对比后发现，尽管使用同一 skill，Meta 报告在以下维度分析密度不足：

1. 护城河模块只有散文，没有五维评分表
2. 缺少多口径估值闸门（峰值/新周期中枢/旧周期平准/EV-FCF）
3. 缺少同业横向对比表
4. Variant View 位置不统一

根因：methodology 已描述这些要求，但都是散文指导，没有 lint 硬约束。LLM 执行时可跳过，无人拦住。

## 目标

把 methodology 里已有的分析密度要求从"建议"升级为"lint 强制门禁"，确保每份报告都执行，不依赖 LLM 自觉。

## 改动范围

### 1. 护城河评分表强制（模块 3）

**现状：** methodology 说"五维打分各 1-10 分，每项给证据"，但无 lint 检查。

**改为：** lint 检查模块 3 必须包含至少 5 行的评分表格，列名含维度/分数/证据。具体维度允许灵活（品牌、网络效应、转换成本、成本优势、技术、资本配置等），但必须量化打分并有证据列。

**lint 规则：** 模块 3 内必须存在一个 Markdown 表格，表格至少 5 行数据行，列名包含"分数"或"score"（不区分大小写），且每行有非空证据列。

### 2. 多口径估值闸门强制（模块 4）

**现状：** methodology 说"对强周期和高 Capex 公司必须输出双估值闸门"，但无 lint 检查。

**改为：** lint 检查模块 4 必须包含估值闸门表格。触发条件：报告提到 capex/capital expenditure/资本开支 且数值 >= $50B（或等价表述），或报告明确标注为周期性行业（存储/能源/航运/银行/汽车/半导体等）。

**lint 规则：** 当 Evidence Ledger 或正文出现 capex >= $50B 或周期性行业关键词时，模块 4 必须包含至少 3 行的估值口径表格，列名含口径/EPS 或 FCF 基数/倍数/适用性。

**非触发：** 非周期、低 capex 公司（如纯软件 SaaS）不强制多口径，TTM + Forward 即可。

### 3. 同业横向对比强制（模块 3 或模块 7）

**现状：** methodology 说"至少选 2-3 个直接竞品横向比"，但无 lint 检查。

**改为：** lint 检查报告必须包含至少一个同业对比表格（2+ 竞品），内容含增速/利润率/市占率/估值/资产负债表中至少 3 项。表格可放在模块 3（护城河直接竞争）或模块 7（机构视角与机会成本）。

**lint 规则：** 报告中必须存在一个表格，表头或首列含至少 2 个非目标公司的竞品名称，且列名含"PE""利润率""增速""市占率""市值"中的至少 2 个。

**例外：** 如果公司无直接竞品（如垄断性国企），可声明"无直接可比竞品"并说明理由，跳过此门禁。

### 4. Variant View 位置统一

**现状：** contract 说放在 Final Verdict 或 module 内，位置模糊。

**改为：** Variant View 统一放在模块 9（最终判决）内，作为 `### Variant View` 子标题。模块 6（物理增长极限）不再放置 Variant View。

**lint 规则：** 模块 9 必须包含 `### Variant View`；模块 6 不得包含 `### Variant View`。

## 不在范围内

- 不改变 9 模块结构
- 不改变 Action Matrix 规则
- 不改变回本测试公式
- 不增加新模块
- 不改变 Evidence Ledger 字段定义

## 验证标准

1. Meta 2026-07-30 报告重跑后通过新 lint
2. SK海力士报告通过新 lint（已有这些结构）
3. 美光报告通过新 lint
4. 76 个现有测试不 break
5. 新增 lint 测试覆盖每个门禁


## 增补条目 5：价格纪律强制门禁 - 2026-07-31

### 背景

Meta 2026-07-30 报告审查发现 module 8（仓位与风控）只有纪律门槛和 Action Matrix，缺了两块 methodology 已要求的价格纪律内容：目标 PE 与价格线、价格区间摘要。SK海力士报告实际包含了四档价格区间表，证明这是历史惯例。根因同前四项：methodology 写了 prose 指导，但 lint 无门禁、模板无占位，LLM 执行时跳过。

### 现状

- `references/full-methodology.md` 在 module 9 节点下用 `####` 列出"目标 PE 与价格线"和"⭐ 价格区间摘要（强制）"
- `templates/full-report.md` 的 module 8 和 module 9 均无这两块占位
- `scripts/report_lint.py` 无针对价格纪律的检查

### 目标

把价格纪律从"建议"升级为 lint 强制门禁，并补齐模板占位，确保每份报告都输出目标 PE、目标价、三档价格区间。

### 改动范围

**Gate 5（report_lint.py）：价格区间摘要强制**

- module 8（仓位与风控）必须包含一个价格区间表，表头含"价格区间"或"区间"，且行名/单元格覆盖"安全边际区""观察区""高估区"三档中的至少两档（允许中英混排：safe/margin、observation、overvalued）。
- 例外：如果报告明确声明该公司无价格区间意义（如已清仓且不打算重新建仓的纯观察标的），可声明"无价格区间"并说明理由跳过。但持仓标的不得使用此例外。

**Gate 6（report_lint.py）：目标 PE 与价格线强制**

- module 8 或 module 9 必须包含目标 PE 或目标价格的量化输出。判定条件：出现"目标 PE""目标价""target PE""target price""安全买入"等关键词，且附近有具体数值（数字 + 货币/倍数）。纯定性描述（如"需要更低价格"）不满足。

**模板补全（templates/full-report.md）**

- 在 module 8 的 Action Matrix 之后补"目标 PE 与价格线"和"价格区间摘要"两个 `###` 子标题占位，与 methodology 对齐。

**methodology 强化（references/full-methodology.md）**

- 把"目标 PE 与价格线"和"价格区间摘要"从 `####` 提升为明确标注 lint 强制要求，指向 `report_lint.py`。

### 不在范围内

- 不改变价格区间只解释估值、不定义交易的原则（交易仍由 Action Matrix 唯一定义）
- 不改变 9 模块结构
- 不新增模块

### 验证标准

1. Meta 2026-07-30 报告补齐价格纪律后通过新 lint
2. SK海力士报告通过新 lint（已有价格区间表）
3. 美光报告通过新 lint（如缺则补）
4. 76+ 现有测试不 break
5. self-test 和 fixtures 通过


## 增补条目 6：金额单位标准化为"亿" - 2026-07-31（完成）

### 背景

Meta 2026-07-30 报告金额单位严重混用：`$130B`、`$1.33T`、`$784M`、`$90.264B`、`$17B` 全是西式 M/B/T 缩写，没有任何一处用"亿"。SK海力士报告（韩元）同样混用 KRW 缩写。单位不统一降低可读性，也让读者无法快速横向比较绝对金额规模。用户要求：到亿的数量级统一用"亿"作为标准单位。

### 标准口径（用户确认）

1. **绝对金额统一用"亿"**：营收、净利润、利润、FCF、OCF、capex、现金、债务、市值、EV 等绝对金额，无论原币种，统一表示为"原币种 + 亿"。不跨币种换算（不引入汇率依赖和审计风险）。
   - 美元：`$130B` -> `$1,300亿`，`$784M` -> `$7.84亿`，`$1.33T` -> `$13,300亿`
   - 人民币：`930亿元`（已符合，保持）
   - 韩元：`KRW 92.1T` -> `KRW 92.1万亿` 或保持韩元原值（韩元面值过大，亿不实用，单独处理见下）
2. **每股金额不转"亿"**：EPS、股价、股息、FCF per share、目标价等每股数值保持原值（`$6.18`、`$585.61`），因为它们是每股口径，转成"亿"无意义。
3. **股数转"亿"**：`2.566B 股` -> `25.66亿股`。
4. **倍数/比率/百分比不变**：PE 21.49x、FCF yield 2.52%、经营利润率 31% 等保持原样。
5. **韩元特例**：韩元面值过大（1 韩元 ≈ 0.005 元），"亿"不实用。韩元金额保持 KRW + 原缩写（T/B），但在报告里附注人民币等值粗算（可选）。韩元报告的"亿"标准不强制。

### lint 规则（Gate 7）

**判定逻辑**：扫描 Evidence Ledger 和正文的绝对金额单元格，检测西式缩写 M/B/T（后接货币符号或明确金额上下文）。如果同一份报告中"亿"和西式缩写同时出现且西式缩写用于绝对金额（非每股、非倍数），判 FAIL。

**例外**：
- 每股金额（含 /share、/股、EPS、price、股息）不触发
- 倍数和比率不触发
- 韩元报告（含 KRW/₩/韩元）的绝对金额不触发
- 模型公式中的 $M 变量符号不触发

### 改动范围

**Gate 7（report_lint.py）：绝对金额单位标准化**
- 扫描表格单元格和正文，检测用于绝对金额的西式缩写（`$NNB`、`$NNM`、`$NNT`、`NN亿元` 混排）
- 当报告中同时存在"亿"和西式缩写用于绝对金额时，判 FAIL
- 给出具体冲突行，引导修正

**模板和方法论更新**
- `templates/full-report.md`：Evidence Ledger 示例值统一用"亿"
- `references/full-methodology.md`：Data Rules 和 Evidence Ledger 段落加入单位标准说明
- `references/report-contract.md`：加入单位标准条款

### 不在范围内

- 不跨币种换算（不引入汇率）
- 不改变每股金额、倍数、比率的表示
- 韩元报告的绝对金额不强制"亿"（面值过大不实用）
- 不改变 Evidence Ledger 字段定义

### 验证标准

1. Meta 2026-07-30 报告统一为"亿"后通过新 lint
2. 76+ 现有测试不 break
3. self-test 和 fixtures 通过
4. SK海力士报告（韩元）不误触发
